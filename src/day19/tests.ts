import { part1, part2 } from "./solution";

const input = ``;

describe("day19", () => {
  it("part1", () => {
    expect(part1(input)).toEqual(0);
  });
  it("part2", () => {
    expect(part2(input)).toEqual(0);
  });
});
